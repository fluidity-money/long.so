import Confirm from "@/components/sequence/Confirm";
import {ConfirmProps} from "@/components/sequence/Confirm";

export default function ConfirmSwapPage(props: ConfirmProps) {
  return <Confirm {...props}/>;
}
