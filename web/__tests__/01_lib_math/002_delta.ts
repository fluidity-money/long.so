
import { getLiquidityForAmounts } from "@/lib/math";

describe("Liquidity math", () => {
  it("Should suggest the correct amount of liquidity", () => {
    // in line with a test in our contracts
  });
});
